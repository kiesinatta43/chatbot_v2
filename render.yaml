services:
  - type: web
    name: design-tech-chatbot
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python -m app.ingest"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: CHAT_BACKEND
        value: extractive
      - key: PYTHON_VERSION
        value: 3.11.9
